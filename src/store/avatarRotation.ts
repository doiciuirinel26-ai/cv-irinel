/** Mutable rotation target driven by GSAP ScrollTrigger (radians, Y axis). */
export const avatarRotation = { y: Math.PI }

/** True once scroll-driven rotation reaches Y = 0. */
export const heroScrollState = { rotationComplete: false }
